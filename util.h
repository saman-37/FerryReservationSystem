#pragma once

#include <fstream>
#include <string>

class Util {
    public:
        Util();        //constructor

        void startup();
        void shutdown();
        void reset();
};
