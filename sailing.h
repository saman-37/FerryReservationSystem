#pragma once

using namespace std;

#include <string>

class sailing{
    private:


    public:
        sailing();
        sailing();

        bool checkExists(string SailingId);
        bool writeSailing(string SailingId, string vesselName);

};
